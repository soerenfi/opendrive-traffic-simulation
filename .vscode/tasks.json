{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build tsim (debug)",
            "type": "shell",
            "command": "cmake ${workspaceRoot} -DBUILD_CONFIG=debug && make -j12",
            "options": {
                "cwd": "${workspaceRoot}/build"
            },
            "targets": [
                "ALL_BUILD"
            ],
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}